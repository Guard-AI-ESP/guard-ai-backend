DATABASE_URL='postgres://<USER>:<PASSWORD>@<HOST>:<PORT>/<DB>'

# Long random secret (64+ chars recommended)
JWT_SECRET='change_me_to_a_very_long_random_secret_value'

ACCESS_TOKEN_TTL_SECONDS='300' # TTL 300 = 5 minutes
REFRESH_TOKEN_TTL_SECONDS='604800' # TTL 604800 = 7 days

RUST_LOG='info'
